
export const getAnnualizedReturn = (n: number) => n * 3600 * 24 * 365;
export const getDailyReturn = (n: number) => n * 3600 * 24;
export const exponent = (input: any) => Math.pow(10, Number(input));
